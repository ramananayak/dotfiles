# mycli for MySQL

[mycli](https://www.mycli.net/) — a MySQL client with auto-completion and syntax highlighting.

History and log paths are configured directly in `.myclirc`:
- `history_file = ~/.cache/mycli/.mycli-history`
- `log_file = ~/.cache/mycli/.mycli.log`

Copy the history file before moving to a new system.
